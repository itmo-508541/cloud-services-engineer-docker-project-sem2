services:
    frontend:
        build:
            context: ./frontend
        ports:
            - "80:80"
        configs:
            - source: default.conf
              target: /etc/nginx/conf.d/default.conf

configs:
    default.conf:
        file: default.conf
